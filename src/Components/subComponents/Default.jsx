import React from "react";

const Default = ({ heading, subText }) => {
  return (
    <div className="default">
      <h2>{heading}</h2>
      <p>{subText}</p>
    </div>
  );
};

export default Default;
